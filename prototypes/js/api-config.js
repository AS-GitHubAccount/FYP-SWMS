/**
 * Only needed if the HTML is NOT served from the same URL as the API.
 *
 * Recommended setup: deploy this repo so Node serves both /api and /prototypes (e.g. Railway
 * with root directory = backend). Then you do not need to change this file.
 *
 * If the UI is on another domain (GitHub Pages, Netlify, etc.), uncomment and set your public API:
 */
// window.__SWMS_API_BASE__ = 'https://YOUR_SERVICE.up.railway.app/api';
